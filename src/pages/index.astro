---
import Layout from "../layouts/Layout.astro";
import RegistryApp from "../components/RegistryApp";

const user = Astro.locals.user;
const isLoggedIn = !!user;
---

<Layout>
  <RegistryApp client:load isLoggedIn={isLoggedIn} userId={user?.id ?? null} />
</Layout>
